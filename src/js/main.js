// Initialize AOS
document.addEventListener('DOMContentLoaded', () => {  // Your existing initialization code
    // Toast functionality
  function showToast(message, type = 'success') {    const toastContainer = document.getElementById('toast-container') || 
                          document.createElement('div');    // Rest of your toast code
  }
});






